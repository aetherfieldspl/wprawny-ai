import { GeneratorWizard } from "@/components/generator/GeneratorWizard";

export const metadata = {
  title: "Generator polityki AI — wprawny.ai",
};

export default function GeneratorPage() {
  return <GeneratorWizard />;
}
